# Optional step1: UDatasets
#
# If you want the runner to call UDatasets for you, put a command here.
# Otherwise keep STEP_UDATASETS_ENABLED=0 in configs/pipeline.env.

# Recommended: write processed jsonl into datapool, then (optionally) sync to OBS.
UDATASETS_OUTPUT_DIR="${DATAPOOL_ROOT}/data/processed"
UDATASETS_OUTPUT_URI="obs://datasets/Toucan-1.5M-HAS/processed/"

# Put your command here (example):
# UDATASETS_CMD="python -m udatasets.cli --in ${DATAPOOL_ROOT}/data/raw --out ${UDATASETS_OUTPUT_DIR} && obsutil cp -r ${UDATASETS_OUTPUT_DIR} ${UDATASETS_OUTPUT_URI}"
UDATASETS_CMD=""

